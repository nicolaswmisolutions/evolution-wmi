services:
  api:
    container_name: evolution_api
    image: evolution/api:local
    build: .
    restart: always
    ports:
      - 8080:8080
    volumes:
      - evolution_instances:/evolution/instances
    networks:
      - evolution-net
    env_file:
      - .env
    expose:
      - 8080

  frontend:
    container_name: evolution_frontend
    image: evolution/manager:local
    build:
      context: ./evolution-manager-v2
      args:
        # Sobe o manager com a API mockada no navegador, sem backend algum.
        # Só para prototipagem — nunca em ambiente exposto.
        VITE_DEMO_MODE: "true"
    restart: always
    ports:
      - "3000:80"
    networks:
      - evolution-net

volumes:
  evolution_instances:


networks:
  evolution-net:
    name: evolution-net
    driver: bridge
